<Window x:Class="CoffeePos.Views.MessageBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoffeePos.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MessageBoxView" Height="150" Width="200" WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" HorizontalAlignment="Right" Width="50" VerticalAlignment="Top" Margin="0" BorderBrush="{x:Null}" Background="{x:Null}" Click="BtnBack_Click">
                <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Foreground="Black"/>
            </Button>
            <TextBlock Grid.Row="1" Margin="0" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Thanh toán thành công" Foreground="Black"/>
            <Button Grid.Row="2" Background="Green" VerticalAlignment="Bottom" Content="Xác nhận" Click="ConfirmPaymentClick" BorderBrush="{x:Null}"></Button>
        </Grid>
        
    </Grid>
</Window>
